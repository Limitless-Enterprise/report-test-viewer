# Purple Basil Medspa — Detailed Revenue Leak Audit

Static publish folder for the client report. Open `index.html` locally or host the repo root on GitHub Pages (or any static host).

**Update the live site:** copy the latest report from the parent folder:

```bash
cp ../detailed-revenue-leak-audit.html index.html
```

Then commit and push.
